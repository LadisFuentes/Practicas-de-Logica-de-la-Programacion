const fs=require("fs");

const mongo= "./mongo.json"

const showUsers=()=>{
try {
    
} catch (error) {
   if(errror) throw new Error(`${error}`)

    console.log(`todos los usuarios\n ${data}`)
    
}

}
module.exports= showUsers